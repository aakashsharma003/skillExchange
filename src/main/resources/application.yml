spring:
  application:
    name: skillexchange

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/skillexchange}
      auto-index-creation: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:as861949578@gmail.com}
    password: ${MAIL_PASSWORD:cflm lkwx hqgq oxms}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
  compression:
    enabled: true

app:
  jwt:
    secret-base64: ${JWT_SECRET:U7/A5+QHKqCFAEECjKa3wK3cpMZb2fBPaaeE20PSpO4=}
    ttl-minutes: ${JWT_TTL_MINUTES:60}
    clock-skew-seconds: 60
  otp:
    ttl-minutes: 5
    length: 6
  mail:
    from: ${MAIL_FROM:as861949578@gmail.com}

logging:
  level:
    root: INFO
    com.skillexchange: DEBUG
    org.springframework.data.mongodb.core: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method